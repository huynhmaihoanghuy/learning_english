import * as types from '../../../action_types/vocabulary';

const aNewWord = (state = [], action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
};

export default aNewWord;