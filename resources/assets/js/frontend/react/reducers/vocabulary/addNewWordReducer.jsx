import * as types from '../../action_types/vocabulary';
const initialAddNewWord = {
    word: '',
    description: ''
};

const addNewWordReducer = (state = initialAddNewWord, action) => {
    switch (action.type) {

    }
    return state;
};

export default addNewWordReducer;